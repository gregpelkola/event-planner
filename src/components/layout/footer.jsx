import React from 'react';
import { Typography, Container } from '@mui/material';

const Footer = () => {
    return (
    <footer>
        <Container maxWidth="sm">
            <Typography variant="body1">Event Planning &copy; 2024</Typography>
        </Container>
    </footer>
    );
};

export default Footer;
